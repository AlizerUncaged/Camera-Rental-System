<UserControl x:Class="Camera_Rental_System.Pages.HomeProducts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Camera_Rental_System.Pages"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/Resources/Cam1.png" Width="250" Height="250" Margin="0,0,25,0">

            </Image>
            <StackPanel MaxWidth="250" Orientation="Vertical" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Reviews " FontSize="16" FontWeight="Bold" Margin="0,0,15,0" />
                    <materialDesign:PackIcon Kind="Star" VerticalAlignment="Center" Foreground="#FFF8BF58" Margin="0,0,10,0"></materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="Star" VerticalAlignment="Center" Foreground="#FFF8BF58" Margin="0,0,10,0"></materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="Star" VerticalAlignment="Center" Foreground="#FFF8BF58" Margin="0,0,10,0"></materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="Star" VerticalAlignment="Center" Foreground="#FFF8BF58" Margin="0,0,10,0"></materialDesign:PackIcon>
                    <materialDesign:PackIcon Kind="StarOutline" VerticalAlignment="Center" Foreground="#FFF8BF58" Margin="0,0,10,0"></materialDesign:PackIcon>
                </StackPanel>
                <TextBlock Text="Canon R" FontFamily="{DynamicResource Bunya Bold}" FontSize="40" Margin="0,0,0,15" FontWeight="Bold"></TextBlock>
                <TextBlock Text="Born to rule." FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <Button materialDesign:ShadowAssist.ShadowDepth="Depth0" Padding="0" BorderBrush="{x:Null}" Style="{DynamicResource MaterialDesignFloatingActionButton}" Background="#FFE3E2E0" Width="35" Height="35" Margin="0,0,25,0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.25" Direction="0"/>
                        </Button.Effect>
                        <materialDesign:PackIcon Kind="HeartOutline" Foreground="#FF2B2B2B" Width="20" Height="20"></materialDesign:PackIcon>
                    </Button>
                    <Button materialDesign:ButtonAssist.CornerRadius="7" materialDesign:ShadowAssist.ShadowDepth="Depth0" BorderBrush="{x:Null}" Background="#FF2B2B2B" Foreground="#FFEBEBEB" Content="Add to cart" Height="35" FontWeight="Bold">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.25" Direction="0"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
